# Host the dev/prod server binds to.
# Default: 127.0.0.1 (loopback only — safe for local use).
# Set to 0.0.0.0 to expose on the local network (not recommended for public exposure).
HOST=127.0.0.1

# Disable Vite HMR during development (set to "true" to turn off hot reload).
DISABLE_HMR=false
